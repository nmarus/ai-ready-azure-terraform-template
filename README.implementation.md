# <Project Name> Azure Infrastructure

[![Terraform](<ci-badge-url>)](<ci-workflow-url>)
[![License: MIT](https://img.shields.io/badge/License-MIT-yellow.svg)](LICENSE)

Brief description of what this workload is, what it deploys, and why it exists.

> **Using this file**: When your workload is ready to go live, copy this file over `README.md`, fill in the placeholders, and delete this callout. The Terraform Reference section at the bottom is auto-generated by `terraform-docs` — leave the `BEGIN_TF_DOCS` / `END_TF_DOCS` markers in place.

## Architecture

High-level description of the resources deployed and how they relate to each other. Update this section as resources are added.

### Deployed Resources

- Resource Group: `rg-<workload>-<env>-<region>`
- _(list key resources here as they are provisioned)_

## Prerequisites

- [Terraform](https://www.terraform.io/) >= 1.14
- [Azure CLI](https://learn.microsoft.com/en-us/cli/azure/)
- Access to the `<subscription-name>` Azure subscription

See [DEPENDENCIES.md](DEPENDENCIES.md) for full tool installation instructions (pre-commit, tflint, checkov).

## Configuration

Copy `terraform.tfvars.example` to `terraform.tfvars` and set your values. `.tfvars` files are gitignored and never committed.

| Variable | Description | Example |
|---|---|---|
| `project` | Project name applied to all tags | `my-project` |
| `workload` | Workload name used in resource naming | `api-backend` |
| `owner` | Team name or distribution list | `platform@company.com` |
| `environment` | Target environment | `prod` |
| `region` | Azure region | `eastus` |

## Environments

| Environment | Status | Notes |
|---|---|---|
| `dev` | Active | |
| `staging` | Active | |
| `prod` | Active | |

## Quick Start

1. `cp terraform.tfvars.example terraform.tfvars`
2. Edit `terraform.tfvars` with your values
3. `az login`
4. `az account set --subscription "<subscription-id>"`
5. `terraform init`
6. `terraform plan -var-file="terraform.tfvars"`
7. `terraform apply -var-file="terraform.tfvars"`

See [DEPLOYMENT.md](DEPLOYMENT.md) for the full deployment guide, including remote state setup and troubleshooting.

## Related Documentation

| Document | Purpose |
|---|---|
| [DEPLOYMENT.md](DEPLOYMENT.md) | Full step-by-step deployment guide |
| [DESIGN.md](DESIGN.md) | CAF naming conventions and tagging standards |
| [DEPENDENCIES.md](DEPENDENCIES.md) | Tool installation instructions |

## Terraform Reference

<!-- BEGIN_TF_DOCS -->
<!-- END_TF_DOCS -->
